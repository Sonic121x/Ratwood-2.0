From 8bf761da0701a897eb8e4ce088432e4bfc2cf9a2 Mon Sep 17 00:00:00 2001
From: Translation Patch Toolkit <translation@local>
Date: Sat, 30 May 2026 13:00:00 +0800
Subject: [PATCH] 翻译 code/modules/mob/dead/new_player/login.dm
MIME-Version: 1.0
Content-Type: text/plain; charset=UTF-8
Content-Transfer-Encoding: 8bit

diff --git a/code/modules/mob/dead/new_player/login.dm b/code/modules/mob/dead/new_player/login.dm
index 685321d96a53..9a7269dee118 100644
--- a/code/modules/mob/dead/new_player/login.dm
+++ b/code/modules/mob/dead/new_player/login.dm
@@ -30,27 +30,27 @@
 		to_chat(src, "<div class=\"motd\">[motd]</div>", handle_whitespace=FALSE)
 
 	if(GLOB.rogue_round_id)
-		to_chat(src, span_info("ROUND ID: [GLOB.rogue_round_id]"))
+		to_chat(src, span_info("轮次 ID：[GLOB.rogue_round_id]"))
 
 	if(client.is_new_player())
-		to_chat(src, span_userdanger("Due to an invasion of goblins trying to play ROGUETOWN, you need to register your discord account or support us on patreon to join."))
-		to_chat(src, span_info("We dislike discord too, but it's necessary. To register your discord or patreon, please click the 'Register' tab in the top right of the window, and then choose one of the options."))
+		to_chat(src, span_userdanger("由于有大批地精试图涌入 ROGUETOWN，你需要绑定 Discord 账号或在 Patreon 上支持我们后才能加入。"))
+		to_chat(src, span_info("我们也不喜欢 Discord，但这是必要措施。若要绑定 Discord 或 Patreon，请点击窗口右上角的“Register”标签，然后选择其中一个选项。"))
 	else
 		var/shown_patreon_level = client.patreonlevel()
 		if(!shown_patreon_level)
-			shown_patreon_level = "<font color='#617C46'><b>Vale Dweller</b></font>"
+			shown_patreon_level = "<font color='#617C46'><b>谷地居民</b></font>"
 		switch(shown_patreon_level)
 			if(1)
-				shown_patreon_level = "Silver"
+				shown_patreon_level = "白银"
 			if(2)
-				shown_patreon_level = "Gold"
+				shown_patreon_level = "黄金"
 			if(3)
-				shown_patreon_level = "Mythril"
+				shown_patreon_level = "秘银"
 			if(4)
-				shown_patreon_level = "Merchant"
+				shown_patreon_level = "商贾"
 			if(5)
-				shown_patreon_level = "Lord"
-		to_chat(src, span_info("Donator Level: [shown_patreon_level]"))
+				shown_patreon_level = "领主"
+		to_chat(src, span_info("赞助等级：[shown_patreon_level]"))
 	client.changelog()
 
 	var/primary_server = "byond://ratwood.rip:22096"
@@ -58,32 +58,32 @@
 	var/link_style = "color:#638500;text-decoration:underline;"
 
 	if(world.port == 22096)
-		to_chat(src, "<span style='color:#638500;'>Secondary Server: [secondary_server] <a href='?src=[REF(src)];join_server=secondary' style='[link_style]'><b>(JOIN)</b></a></span>")
+		to_chat(src, "<span style='color:#638500;'>副服务器：[secondary_server] <a href='?src=[REF(src)];join_server=secondary' style='[link_style]'><b>(加入)</b></a></span>")
 	else if(world.port == 22099)
-		to_chat(src, "<span style='color:#638500;'>Primary Server: [primary_server] <a href='?src=[REF(src)];join_server=primary' style='[link_style]'><b>(JOIN)</b></a></span>")
+		to_chat(src, "<span style='color:#638500;'>主服务器：[primary_server] <a href='?src=[REF(src)];join_server=primary' style='[link_style]'><b>(加入)</b></a></span>")
 	else
-		to_chat(src, "<span style='color:#638500;'>Primary Server: [primary_server] <a href='?src=[REF(src)];join_server=primary' style='[link_style]'><b>(JOIN)</b></a></span>")
-		to_chat(src, "<span style='color:#638500;'>Secondary Server: [secondary_server] <a href='?src=[REF(src)];join_server=secondary' style='[link_style]'><b>(JOIN)</b></a></span>")
+		to_chat(src, "<span style='color:#638500;'>主服务器：[primary_server] <a href='?src=[REF(src)];join_server=primary' style='[link_style]'><b>(加入)</b></a></span>")
+		to_chat(src, "<span style='color:#638500;'>副服务器：[secondary_server] <a href='?src=[REF(src)];join_server=secondary' style='[link_style]'><b>(加入)</b></a></span>")
 
-	to_chat(src, "<a href='?src=[REF(src)];open_changelog=1' style='color:#638500;text-decoration:underline;'><b>Open Changelog</b></a>")
+	to_chat(src, "<a href='?src=[REF(src)];open_changelog=1' style='color:#638500;text-decoration:underline;'><b>打开更新日志</b></a>")
 
 	if(GLOB.admin_notice)
-		to_chat(src, span_notice("<b>Admin Notice:</b>\n \t [GLOB.admin_notice]"))
+		to_chat(src, span_notice("<b>管理员通知：</b>\n \t [GLOB.admin_notice]"))
 
 	var/spc = CONFIG_GET(number/soft_popcap)
 	if(spc && living_player_count() >= spc)
-		to_chat(src, span_notice("<b>Server Notice:</b>\n \t [CONFIG_GET(string/soft_popcap_message)]"))
+		to_chat(src, span_notice("<b>服务器通知：</b>\n \t [CONFIG_GET(string/soft_popcap_message)]"))
 
 	if(SSticker.current_state < GAME_STATE_SETTING_UP)
 		var/tl = SSticker.GetTimeLeft()
 		var/postfix
 		if(tl > 0)
-			postfix = "in about [DisplayTimeText(tl)]"
+			postfix = "约在 [DisplayTimeText(tl)] 后"
 		else
-			postfix = "soon"
-		to_chat(src, "The game will start [postfix].")
+			postfix = "很快"
+		to_chat(src, "游戏将于 [postfix] 开始。")
 
 		SSvote.send_vote(client)
 		var/usedkey = ckey(key)
-		var/list/thinz = list("takes a seat.", "settles in.", "joins the session", "joins the table.", "becomes a player.")
+		var/list/thinz = list("入座了。", "安顿好了。", "加入了本局。", "来到桌前。", "成为了一名玩家。")
 		SEND_TEXT(world, span_notice("[usedkey] [pick(thinz)]"))
-- 
Translation Patch Toolkit
